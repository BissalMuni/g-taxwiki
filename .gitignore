node_modules
.next
.env
.env.local
.env*.local
*.tsbuildinfo

.claude/settings.local.json
.claude/*.lock
tsconfig.tsbuildinfo

# 검색 인덱스 (빌드 산출물 — pnpm search:index 로 자동 생성)
public/search-index.json

# Docker Claude
Dockerfile.claude
docker-compose.claude.yml
.claude-output/

# 실무 원본 자료 (PDF/HWPX 등 로컬 참고용)
silmu/
*.pdf
.vercel
